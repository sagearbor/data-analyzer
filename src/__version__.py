"""
Single source of truth for the application version.

Imported by web_app.py (About tab, navbar subscript) so the displayed
version never drifts out of sync with a hardcoded string in the UI.
"""

__version__ = "2.2.0"
