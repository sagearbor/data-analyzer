# Web Application Requirements
# Pinned exact (not >=): the custom navbar CSS in web_app.py targets
# Streamlit's internal DOM structure, which has changed across minor
# versions (e.g. .stTabs [data-baseweb="tab-list"] no longer matches the
# pre-1.30 layout). Bump deliberately and re-verify the navbar when upgrading.
streamlit==1.59.2
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.15.0
python-multipart>=0.0.6
openpyxl>=3.1.2
xlrd>=2.0.1
pyarrow>=14.0.0
chardet>=5.2.0
matplotlib>=3.7.0
pypdf>=4.0.0
requests>=2.31.0  # HTTP client for calling the data-analyzer REST API (see DataQualityAnalyzer in web_app.py)

# LLM Integration Requirements
openai>=1.0.0
tiktoken>=0.5.0
python-dotenv>=1.0.0

# Environment Banner (will auto-update when new versions published to PyPI)
env-banner>=0.1.0
